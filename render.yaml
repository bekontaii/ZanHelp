services:
  - type: web
    name: backend
    env: go
    buildCommand: go build -o app
    startCommand: ./app

  - type: web
    name: frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: build
